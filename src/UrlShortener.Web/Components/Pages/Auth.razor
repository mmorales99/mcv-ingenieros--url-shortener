@page "/dashboard"

@using Microsoft.AspNetCore.Authorization

@attribute [Authorize]

<PageTitle>Dashboard</PageTitle>

<h1>Dashboard</h1>

<AuthorizeView>
    <p>Signed in as <strong>@context.User.Identity?.Name</strong>.</p>
    <p>The link management features will be added in the next implementation phases.</p>
</AuthorizeView>
